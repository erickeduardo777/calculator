import { create } from "zustand";

const useStore = create((set, get) => ({
    name: "erick eduardo souza de araujo aquele que reina como uma aguia rei eterno governante eterno",
}))

export default useStore